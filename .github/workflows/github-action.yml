name: Time Wasting Test Placeholder

on:
  push:
    branches: [ ]

jobs:
  wait:
    runs-on: ubuntu-latest

    steps:
      - name: wait-step
        run: |
          echo Sleeping...
          sleep 10
          echo Nap time over.
        
